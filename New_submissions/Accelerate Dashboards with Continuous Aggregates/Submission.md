Our team completed the Continuous Aggregates challenge for MLH Global Hack Week: Data. We created a continuous aggregate named conditions_hourly on the conditions hypertable in our Tiger Cloud TimescaleDB service. This pre-computes hourly metrics including average, min, and max temperature and humidity, along with reading counts per device. The aggregate enables fast dashboard queries by calculating results in the background instead of scanning the full table on every request.